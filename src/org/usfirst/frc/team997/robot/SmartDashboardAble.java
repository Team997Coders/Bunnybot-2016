package org.usfirst.frc.team997.robot;

/** An interface providing access to a single function, `smartDashboard()`, that allows
 * uniform access to classes that want access to the Smart Dashboard. */
public interface SmartDashboardAble {
	/** Log information to the Smart Dashboard. */
	public void smartDashboard();
}
